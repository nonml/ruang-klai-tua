# เรื่องใกล้ตัว — Civic Info Hub (ไทย) 🧩📍

แพลตฟอร์มแบบ **PWA** สำหรับประชาชนในไทย เพื่อ **ติดตาม/รายงาน/แชร์หลักฐาน “เรื่องใกล้ตัว”** (เริ่มจากหมวดโรงเรียน แล้วค่อยขยายไปหมวดอื่น) ด้วยข้อมูลที่ตรวจสอบได้ โดยยึดหลัก **ไม่กล่าวหา ไม่จัดอันดับโกง** และออกแบบให้ **ปลอดภัยต่อผู้ใช้/ผู้ดูแล** (PDPA + หมิ่นประมาท + ความเสี่ยงทางสังคม)

> ✅ *เน้น “ข้อเท็จจริงจากหลักฐาน ณ วันที่…”*  
> ✅ *ทุกโพสต์เริ่มเป็น “ยังไม่ยืนยัน”*  
> ✅ *มีระบบ auto-blur + auto-hold + auto-takedown*  
> ✅ *Login ก่อน (LINE) เพื่อ trace ได้*  
> ✅ *Open-source เพื่อให้ dev มาช่วยได้จริง (AGPL)*

---

## 1) เป้าหมาย (Goals)

### เป้าหมายหลัก
- ทำให้คนทั่วไป **ส่งหลักฐานได้ง่าย** (ถ่ายรูป + พิกัด + เลือกประเภท + ข้อความสั้น)
- ทำให้คนทั่วไป **ค้นหา/ดูใกล้ฉัน** ได้ (แผนที่ + รายการ + แชร์การ์ดเข้า LINE)
- ลดความเสี่ยง: **ไม่ให้กลายเป็นเวทีกล่าวหา**
- ทำให้ “ตรวจแล้ว” เกิดได้โดยใช้คนต่ำสุด: **crowd confirmation + trusted tier + LLM risk gate**

### Non-goals (ไม่ทำในระยะแรก)
- ไม่ทำ “จัดอันดับโกง / ฟันธงทุจริต”
- ไม่ทำ doxxing/เปิดข้อมูลส่วนบุคคล
- ไม่ทำคะแนนความดี/ความเลวของบุคคล/โรงเรียน
- ไม่ทำการเมืองพรรค/ผู้สมัคร (โฟกัสโรงเรียนเท่านั้น)

---

## 2) ขอบเขต MVP (2 สัปดาห์) — *เน้นให้ “มีของจริง” เร็วที่สุด* ⏱️

> MVP รอบแรกโฟกัส **หมวดโรงเรียน** เพื่อพิสูจน์ระบบ จากนั้นค่อยเปิดเป็น multi-purpose เต็มรูปแบบ

### MVP Features
1) **Login ด้วย LINE** (อย่างน้อยต้อง trace ผู้โพสต์ได้)
2) หน้า **ใกล้ฉัน**: แผนที่ + รายการเคสล่าสุด
3) หน้า **ส่งหลักฐาน**: ฟอร์ม 7 ช่อง + อัปโหลดรูป
4) หน้า **ดูเคส**: รูป/รายละเอียด/สถานะ/ปุ่ม “ยืนยัน” และ “รายงาน”
5) **ระบบสถานะเคส**
   - `PENDING` = ยังไม่ยืนยัน (default)
   - `VERIFIED` = ตรวจแล้ว (ตามกติกายืนยัน)
   - `HELD` = ถูก hold เพราะ risk สูง (อัตโนมัติ)
   - `HIDDEN` = ถูกซ่อน (รายงานถึง threshold หรือ policy)
6) **Auto-blur** ก่อนจัดเก็บ (หน้า/ทะเบียน/บ้านเลขที่/ข้อมูลเด็ก/ข้อมูลส่วนบุคคล)
7) **Risk gate (LLM/Rules)**
   - ตรวจข้อความ/ผล OCR ว่ามี PII/การกล่าวหา/คำเสี่ยง → `HELD`
8) **Crowd confirmation**
   - เปลี่ยนเป็น `VERIFIED` เมื่อได้ **2 การยืนยันจากบัญชีต่างกัน**
   - (รองรับ trusted tier ในเฟสถัดไป)

### ฟอร์ม 7 ช่อง (MVP)
- รูปหลักฐาน 1–3 รูป *(บังคับ)*
- พิกัด *(บังคับ)*
- โรงเรียน *(เลือกจากค้นหา + พิมพ์ได้)*
- ประเภทปัญหา *(dropdown)*
- ความเร่งด่วน *(ต่ำ/กลาง/สูง)*
- “สิ่งที่เห็น” *(preset + ข้อความสั้น <= 200 ตัวอักษร)*
- วันที่พบ *(auto วันนี้, แก้ได้)*

### ประเภทปัญหา (MVP dropdown)
- ห้องน้ำ/สุขาภิบาล
- น้ำดื่ม/น้ำใช้
- ไฟฟ้า/แสงสว่าง
- อาคารชำรุด/อันตราย
- อุปกรณ์เรียน/ห้องเรียน
- ความปลอดภัยหน้าโรงเรียน/ทางม้าลาย/การจราจร
- อาหารกลางวัน *(เชิงสังเกต — ห้ามกล่าวหา)*

---

## 3) Guardrails (ไทยเท่านั้น) — ความปลอดภัย/กฎหมาย 🧱🇹🇭

### 3.1 PDPA / การคุ้มครองข้อมูลส่วนบุคคล
- บังคับ **auto-blur** ทุกใบหน้า และสิ่งที่เสี่ยง (ป้ายทะเบียน, บ้านเลขที่, ชื่อเด็ก, เบอร์โทร, เลขบัตร ฯลฯ)
- ห้ามใส่ “ชื่อบุคคล/ครู/เด็ก” ในข้อความ → ถ้าพบให้ **hold** อัตโนมัติ

### 3.2 หมิ่นประมาท / กล่าวหา
- หลีกเลี่ยงคำกล่าวหา เช่น “โกง”, “กินเงิน”, “ยักยอก”
- UI/Copy ใช้รูปแบบ: **“ข้อมูลจากป้าย/เอกสาร/ภาพ ณ วันที่…”**
- โพสต์ที่มีคำเสี่ยง → `HELD` (อัตโนมัติ)

### 3.3 Policy การซ่อน/ลบ (Takedown)
- **Auto-hide ภายใน ~10 นาที** เมื่อ
  - มีรายงาน ≥ **3 คน** (บัญชีต่างกัน) หรือ
  - Risk score สูง (LLM/Rules)
- เคสที่ถูก hold/hide: ไม่ขึ้นหน้าใกล้ฉัน/ค้นหา และแชร์การ์ดไม่ได้

> หมายเหตุ: โครงการนี้เป็น “ระบบรวบรวมข้อมูลสาธารณะ/หลักฐาน” ไม่ใช่สื่อกล่าวหา  
> ผู้ใช้ต้องยอมรับ Terms ก่อนใช้งาน

---

## 4) สถาปัตยกรรม (เวอร์ชันเริ่มต้น) 🧠

- **Frontend:** Next.js (App Router) + PWA
- **Auth:** LINE Login (LIFF) *(สเกลตันใน repo)*
- **Database:** Supabase Postgres + PostGIS
- **Storage:** Supabase Storage (รูปภาพที่ผ่าน blur แล้ว)
- **API:** Next.js Route Handlers
- **Risk & Blur:** สเกลตัน (วางจุดต่อ LLM/vision/OCR ได้)

> Repo นี้เป็น “เริ่มได้เลย” แต่บางจุดเป็น TODO (เช่น verify LINE token, pipeline blur จริง) เพื่อให้คุณเสียบ provider/บริการที่คุณเลือกได้

---

## 5) ตั้งค่าและรัน (สำหรับ dev) 🛠️

### 5.1 Prerequisites
- Node.js 20+
- pnpm (แนะนำ) หรือ npm
- Supabase project (หรือ local supabase)

### 5.2 Environment variables
สร้างไฟล์ `.env.local`:
```bash
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Firebase client
NEXT_PUBLIC_FIREBASE_API_KEY=...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=...
NEXT_PUBLIC_FIREBASE_PROJECT_ID=...
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=...
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...

# Server (Firebase Admin)
# แนะนำ: ใช้ Application Default Credentials บน Cloud Run/Functions
# (Local) ใส่ service account JSON แบบ string
FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'
```


### 5.3 Install / Run
```bash
pnpm install
pnpm dev
```

### 5.4 ตั้งฐานข้อมูล
- ใช้ไฟล์ SQL ใน `supabase/migrations/0001_init.sql`
- แล้วรันใน Supabase SQL editor (หรือ supabase cli)

---

## 6) โครงสร้าง repo 📦

```
civic-info-hub-th/
  app/                  # Next.js app router
  components/
  lib/
  supabase/migrations/  # SQL schema
  docs/                 # เอกสารสเปค/roadmap
  .github/workflows/    # CI
```

---

## 7) Roadmap ละเอียดยาว (0–24 เดือน) 🗺️

> คีย์เวิร์ด: *เริ่มเล็ก → วัดผล → ลดความเสี่ยง → ค่อยขยาย*

### Phase 0 — สัปดาห์ 0–2 (MVP Pilot)
**Outcome:** มีเคสจริง ≥ 30 เคส ที่ “รูป+พิกัดครบ” และระบบไม่พัง  
**งานหลัก**
- [ ] Login LINE + consent (Terms/PDPA)
- [ ] ฟอร์ม 7 ช่อง + upload + แผนที่ใกล้ฉัน
- [ ] สเตตัส `PENDING/VERIFIED/HELD/HIDDEN`
- [ ] report/confirm
- [ ] baseline risk rules (keyword + PII regex)
- [ ] auto-blur placeholder (pipeline hook)
- [ ] LINE OA template: การ์ดแชร์ (share card)

**Metric**
- ≥ 30 เคส / 14 วัน
- ≥ 20% กลับมาใช้อีกใน 7 วัน
- < 5% เคสที่เข้าข่ายเสี่ยงแล้วหลุดออกสาธารณะ

---

### Phase 1 — เดือน 1–3 (V0.2 “ใช้ได้จริง”)
**Outcome:** ลด friction + เพิ่มคุณภาพข้อมูล โดยไม่เพิ่มคนดูแล

**Product**
- “ค้นหาโรงเรียน” ดีขึ้น: autocomplete + แก้ชื่อซ้ำ (entity resolution เบื้องต้น)
- หน้า “เคสของฉัน” + ติดตามสถานะ
- การ์ดแชร์ที่มี “สรุปสั้น + แหล่งหลักฐาน”
- Trusted tier v1 (คนยืนยันถูกบ่อยขึ้นน้ำหนัก)

**Data/Tech**
- OCR pipeline (อ่านจากภาพป้าย/เอกสาร)
- Blur จริง (face/plate/text PII) ก่อนเก็บ
- Duplicate detection (ภาพคล้าย + พิกัดใกล้กัน + เวลาใกล้กัน)

**Policy**
- แบบฟอร์มข้อความเปลี่ยนเป็น preset มากขึ้น (ลดภาษากล่าวหา)
- ระบบ “ขอแก้ไข” แทนการคอมเมนต์

**Metric**
- Verified rate ≥ 30% ของโพสต์ทั้งหมด
- Duplicate ลดลง ≥ 40%
- เวลาจากโพสต์ → verified เฉลี่ย < 72 ชม.

---

### Phase 2 — เดือน 3–6 (V0.5 “เชื่อมการช่วยเหลือ”)
**Outcome:** จาก “รายงาน” ไปสู่ “ขอความช่วยเหลือ” อย่างปลอดภัย

**Product**
- “ขอความช่วยเหลือ” (ในกรอบที่ปลอดภัย):  
  - template ขอความช่วยเหลือ / แจ้งหน่วยงาน (ไม่กล่าวหา)
- “สถานะการแก้ไข” (ผู้ใช้/โรงเรียน/หน่วยงานอัปเดตได้แบบมีหลักฐาน)
- “สรุปพื้นที่” รายเขต/อำเภอ (ไม่จัดอันดับโรงเรียน/คน)

**Tech**
- Notification ผ่าน LINE OA (เมื่อเคสคุณถูกยืนยัน/ถูกซ่อน/มีอัปเดต)
- Role-based access (เช่น เจ้าหน้าที่/ครูที่ยืนยันตัวตน)

**Metric**
- เคสที่มีอัปเดตสถานะ ≥ 15%
- การรายงานผิด policy ลดลงต่อเนื่อง

---

### Phase 3 — เดือน 6–12 (V1.0 “โครงสร้างมาตรฐาน”)
**Outcome:** ระบบมีมาตรฐานที่ dev คนอื่นเข้ามาช่วยได้จริง + deploy ง่าย

**OSS / DevEx**
- Docs ครบ: architecture, data model, threat model, runbook
- Git workflow เข้ม: CI, lint, typecheck, test, PR gates
- Modularization: แยก `risk`, `blur`, `ocr` เป็น adapters
- Observability: basic logs + metrics dashboard

**Product**
- Offline-friendly PWA
- Local “collections” (กลุ่มเคสประเด็นเดียว) เพื่อแชร์เข้า LINE
- Multi-language (TH เป็นหลัก, EN เป็นรอง)

**Metric**
- Contributor นอกทีม ≥ 3 คน/ไตรมาส
- Release cadence สม่ำเสมอ (เช่นทุก 2 สัปดาห์)

---

### Phase 4 — เดือน 12–18 (Scale + Partnerships)
**Outcome:** มี “ผู้เล่น” นำไปใช้จริง (NGO/สื่อ/ชุมชน) โดยความเสี่ยงไม่พุ่ง

**Partnership**
- คู่มือให้เพจ/ชุมชน “เริ่มใช้ในพื้นที่” (playbook)
- API read-only สำหรับสื่อ/นักวิจัย (หลังทำ privacy filter)
- Data export แบบ anonymized

**Tech**
- Rate limiting / abuse prevention
- Federation-friendly design (หลาย instance ได้)

**Metric**
- DAU/WAU โตแบบยั่งยืน
- Incident (ผิด policy) อยู่ในระดับควบคุมได้

---

### Phase 5 — เดือน 18–24 (Ecosystem / Plugins)
**Outcome:** กลายเป็น “แพลตฟอร์ม” ที่เสียบโมดูลอื่นได้ (เช่น งบ/จัดซื้อ) แต่ยังปลอดภัย

**Plugins**
- Procurement (อ่านประกาศจัดซื้อ/สัญญา) แบบ “เอกสารเท่านั้น”
- School resources dashboard (ข้อมูลครู/ขนาดโรงเรียน) จากโอเพ่นดาต้า
- Fact cards / explainers (อ้างอิงแหล่ง)

**Governance**
- เพิ่ม maintainers + RFC process
- Security policy / responsible disclosure

---

## 8) แนวคิดฐานข้อมูล (สรุป)
ตารางหลัก:
- `users` ผู้ใช้ (LINE subject id)
- `schools` โรงเรียน (ชื่อ/พิกัด/รหัสถ้ามี)
- `reports` เคสหลักฐาน
- `report_votes` การยืนยัน/คอนเฟิร์ม
- `report_flags` การรายงาน
- `attachments` รูป/ไฟล์ (หลัง blur)
- `audit_log` บันทึกเหตุการณ์สำคัญ

ดู SQL: `supabase/migrations/0001_init.sql`

---

## 9) Contribution (สั้น)
- เปิด issue ก่อนถ้าเป็น feature ใหญ่
- PR ต้องผ่าน CI: lint/typecheck/tests
- ใช้ Conventional Commits (`feat:`, `fix:` …)
ดูรายละเอียดใน `CONTRIBUTING.md`

---

## 10) License
- โค้ด: **AGPL-3.0** (ดู `LICENSE`)
- ข้อมูลผู้ใช้: **CC BY 4.0** *(แต่มีสิทธิ์ลบ/ซ่อนตาม policy)*

---

## TL;DR สำหรับผู้ใช้ทั่วไป (ข้อความที่ใช้ใน LINE OA)
> “ที่นี่คือที่รวม **หลักฐานเรื่องใกล้ตัว** ใกล้คุณ (เริ่มจากหมวดโรงเรียน)  
> ทุกโพสต์จะขึ้นเป็น ‘ยังไม่ยืนยัน’ ก่อน และเราจะเบลอข้อมูลส่วนบุคคลให้อัตโนมัติ  
> ช่วยกันส่งหลักฐาน/ยืนยัน เพื่อให้ข้อมูลชัดขึ้น โดยไม่กล่าวหาใคร”


## 11) อัปโหลดรูปแบบปลอดภัย (GCP)
- ใช้ `POST /api/uploads/signed-url` เพื่อขอ signed URL แล้วอัปโหลดเข้า bucket TEMP
- Cloud Function `blurAndPublish` จะทำ blur แล้วคัดไป bucket PUBLIC
- ดูรายละเอียด: `docs/DEPLOY_GCP.md` และ `docs/GCP_PIPELINE_BLUR.md`
